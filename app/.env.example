# Solana Devnet Configuration
DEVNET_RPC=https://api.devnet.solana.com

# Token Mint Addresses (populated by create-mints.ts)
MINT_USDC=
MINT_USDT=
MINT_FLEX=

# Program Addresses (populated by deploy-programs.ts)
PROGRAM_BASKET=
PROGRAM_STRATEGY=
PROGRAM_REBALANCE=

# Wallet Configuration (populated by scripts)
WALLET_PRIVATE_KEY=
WALLET_PUBKEY=

# Frontend Configuration
NEXT_PUBLIC_SOLANA_NETWORK=devnet
NEXT_PUBLIC_APP_NAME=FlexiYield MVP
NEXT_PUBLIC_APP_DESCRIPTION=5-day Solana devnet MVP for basket token rebalancing

# Optional: Custom RPC endpoint (comment out to use default devnet)
# DEVNET_RPC=https://your-rpc-endpoint.com

# Security: Never commit real private keys to version control
# Use environment variables or secret management in production
